# Build

Current build

```powershell
pwsh ./build.ps1 -target build
```

Better build

```powershell
# pwsh .\build.dntool.ps1 -target "build"
dotnet-cake setup.cake --target="build"
```
